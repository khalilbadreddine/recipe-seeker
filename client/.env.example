# Copy to .env and fill in. Never commit real keys.
# Supabase is OPTIONAL — without these the site runs fully on localStorage
# (auth UI stays hidden). Get values from Supabase → Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
